name: "Repo Visibility Policy"
description: "Ensures repository visibility follows organization standards."

resource: "repository"

configuration:
  repoVisibilitySettings:
    active: true
    default: "private"
    exceptions:
      - "public-repos-list"
